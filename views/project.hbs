<h1>Project</h1>

{{#if project}}
{{#if isLoggedIn}}

<form action="/projects/project/delete/{{project.id}}" method="POST" > <input type="submit" value="DELETE" class="btn"></form>
<form action="/edit-project/{{project.id}}" method="GET" > <input type="submit" value="EDIT" class="btn" id="edit-btn"></form>

{{/if}}
    <p>The project {{project.name}} was created {{project.date}}. </p>
    <p>{{project.content}}</p>
    <a href="{{project.link}}">Link to the project</a>
    
{{else}}
<p>That project does not exist.</p>
{{/if}}
