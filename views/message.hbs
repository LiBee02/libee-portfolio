<h1>Client</h1>

{{#if message}}
    <form action="/messages/message/delete/{{message.id}}" method="POST" ><input type="submit" value="DELETE" class="btn"></form>

    <p>{{message.firstname}} {{message.lastname}}</p>
    <p>Email: {{message.email}}</p>
    <p>Phone: {{message.phone}}</p>
    <p>Message: {{message.message}}</p>

{{else}}
<p>That client does not exist.</p>
{{/if}}
